/**
 * Pure domain logic, shared with the web app.
 *
 * Nothing here may import React, React Native, Next.js or @supabase/*. The rule is
 * enforced by ESLint, not by convention — see eslint.config.js.
 *
 * Every file in this package is a TRACKED COPY of a file in rubanwd/slay-city, not
 * an independent implementation. Fix shared bugs upstream, then sync. See docs/SYNC.md.
 *
 * Populated by WP-0.2.
 */
export {};
