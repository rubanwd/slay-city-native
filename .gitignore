node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.DS_Store
.env*.local
npm-debug.*
yarn-debug.*
yarn-error.*

# A read-only checkout of rubanwd/slay-city, fetched on demand.
# Never committed: a snapshot in git rots, and porting from stale code
# reproduces behaviour that no longer exists. See docs/SYNC.md.
upstream/

# Native project folders — generated by prebuild, not source.
/ios
/android
